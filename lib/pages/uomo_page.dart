import 'package:flutter/material.dart';

class UomoPage extends StatelessWidget {
  const UomoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Uomo Page'),
      ),
    );
  }
}
